# Collab — Claude Code Engineering Notes

## Code Review Discipline (Spirefy Engineering Standard)

The full discipline — the North Star prime directive, the 6 mandatory + 4
expansion lens panel, the Goal-Conformance Brief, the Precision Counterweight,
the pre-panel consumer-grep gate, the sibling-class hunt, the
deferred-disposition audit, and the acceptance gate — is the **single source of
truth** at `~/projects/docs/CODE_REVIEW_DISCIPLINE.md` (owned + version-controlled
in the `docs` repo) and governs this repo verbatim.

**Before any substantive code change or review, read that file and apply it.**
The discipline is intentionally NOT duplicated here — edit it there so every repo
stays in lockstep. (Working in this repo standalone without `docs/` checked out?
Fetch `CODE_REVIEW_DISCIPLINE.md` from the `docs` repo — the canonical home.)

## Repo-specific notes

(Capture as discovered.)
