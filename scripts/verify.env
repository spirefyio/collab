# verify.env — sourced by verify.sh. The artifact-affecting build flags that the
# LOCAL build and the CI manifest cell must share, or their artifact sets diverge
# and verify.sh false-reports. collab's CI builds with -Dllama_cpp=false (build.yml),
# so the local verify build must match it.
LOCAL_BUILD_ARGS="-Dllama_cpp=false"
