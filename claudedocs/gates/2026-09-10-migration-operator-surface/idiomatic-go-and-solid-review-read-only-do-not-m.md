# Idiomatic-Go and SOLID review, read-only. Do not modify any file.

_Gate lens on collab@fdc4a7a, 2026-09-10. Source transcript: aa348a32c5fb42a1c.output_

## Scope and method

Worktree HEAD verified as `fdc4a7a77cd924455d085c70f058ef6abb4ce0f4`, exactly one commit ahead of `ab130be` (`git log ab130be..HEAD` shows only this commit). Tree clean. Read `migrate.go`, `main.go`, `migrate_test.go`, `migrate_integration_test.go` in full; diffed `Makefile`/`README.md`/`Dockerfile` for the cross-cutting items. Cross-checked behavior against the pinned dependencies actually resolved in `go.sum` — `golang-migrate/migrate/v4@v4.19.1` and `jackc/pgx/v5@v5.9.2` — plus Go 1.26's `database/sql` stdlib source. Ran `go build ./...`, `go vet ./...`, `gofmt -l`, `go test -v ./internal/db/...` (results below). Deliberately did **not** run `make test-integration`: `docker-compose.yml` pins a fixed, non-project-scoped `container_name: collab-postgres` on host port 5432, which risks colliding with a real `collab-postgres` container from the user's actual `~/projects/collab` checkout. So Steps/Goto/Force/dirty-schema behavior below is **CONFIRMED-BY-READING** against the exact pinned dependency source, not CONFIRMED-BY-MEASUREMENT against a live Postgres.

## Findings

**[LOW] `server/internal/db/migrate.go:70`** — wrong object closed on the `NewWithInstance` error branch. `sqlDB.Close()` runs instead of `driver.Close()`, contradicting the ownership rule stated two branches above at lines 62-63 ("past this point the driver owns it"). By this point `driver` (from the successful `WithInstance` at line 60) already wraps a live, checked-out `*sql.Conn` (acquired via `instance.Conn(ctx)` inside `WithInstance`, for advisory locking) in addition to `sqlDB`. Concrete wrong behavior: none today — `migrate.NewWithInstance` in the pinned v4.19.1 unconditionally does `return m, nil` (verified by reading its body), so this branch is dead code. If a future golang-migrate version starts using the error its own signature already promises, cleanup here stops matching the file's own belt-and-braces pattern. Fix: `driver.Close()` in place of `sqlDB.Close()`. Disposition: CONFIRMED-BY-READING; not reachable today.

**[LOW] `server/internal/db/migrate.go:90`** — `Close()`'s combined error drops the chain: `fmt.Errorf("close migrator: source: %v, database: %v", srcErr, dbErr)` uses `%v` for both. Go 1.20+ (module targets 1.25) supports multiple `%w` verbs in one `fmt.Errorf`, producing `Unwrap() []error` so `errors.Is`/`errors.As` can see both underlying errors (confirmed via `go doc fmt Errorf`). Concrete wrong behavior: none today — every caller of `Close()` either discards it via bare `defer mg.Close()` (RunUp, main.go's `run()`) or only logs it (test cleanup); nothing inspects it programmatically. This is the one spot in the file not using `%w` where it could. Disposition: CONFIRMED-BY-READING; no current caller exercises the gap.

**[MED] `server/cmd/collab-migrate/main.go:175-206`** — `printStatus` mixes data-normalization with presentation and is untestable where it sits. It (1) gathers `Head`/`Version`/`PendingAfter` and normalizes the `ErrNoVersion` sentinel into display zero-values (lines 181-187 — the only place in the diff doing this translation), and (2) formats/prints. (2) is correctly CLI work. (1) is exactly the "operator surface" logic `Migrator`'s own doc comment claims to own, and every other `Migrator` verb has direct coverage in `internal/db` (9 test functions including a real-Postgres round trip). `printStatus` has none, and structurally can't: `Migrator`'s fields are unexported, so `main_test.go` has no seam to fake one — only `db.NewMigrator`, which requires a live connection. Concrete wrong behavior: none today (the logic matches the integration tests' independent verification of `ErrNoVersion` across 5 sites), but a future inversion of that branch has zero test to catch it — it would only surface as an operator misreading `collab-migrate version` output. Related, same root cause: `cmd/collab-migrate` ships zero tests in this diff even though `run(args, url, confirm, dir)` (main.go:70) is already a clean args-in/error-out function ready to be called directly, and `positiveInt` (main.go:208) is pure and trivially testable. Suggested fix: a `Migrator.Status()`-shaped method owning the `ErrNoVersion` normalization, tested alongside its siblings in `internal/db`; `printStatus` then only formats. Disposition: CONFIRMED-BY-READING (design/testability gap), not a functional defect.

**[LOW] `server/internal/db/migrate_integration_test.go:364-368`** — dirty-schema assertion matches dependency error text instead of type: `strings.Contains(strings.ToLower(err.Error()), "dirty")` against `mg.Up()`'s error, rather than `errors.As(err, &migrate.ErrDirty{})` (an exported struct built for exactly this in the dependency). Concrete wrong behavior: none today. If golang-migrate reworded `ErrDirty.Error()`, this assertion silently stops testing what its own comment claims ("Up against a dirty schema should say so"); `errors.As` would survive that. Disposition: CONFIRMED-BY-READING; test-robustness only.

No CRITICAL or HIGH findings.

## Answers to the specific judgment questions

**Item 2 — `NewMigrator` early-return paths, stated explicitly (migrate.go:45-75):**
1. `url == ""` (46): returns `nil, err`. Nothing opened.
2. `iofs.New` fails (50): returns `nil, err`. Nothing opened yet.
3. `sql.Open` fails (55): returns `nil, err`. `sql.Open` doesn't dial; on error no `*sql.DB` is allocated at all.
4. `migratepgx.WithInstance` fails (60): returns `nil, err`, `sqlDB.Close()` explicit (64) — correct, and exercised: `TestRunUp_HandlesUnreachableDB` and `TestRunUp_AcceptsCustomFS` both hit exactly this branch (unreachable `192.0.2.1`) and both **measured PASS** (see test run below). Caveat, not this code's bug: if `WithInstance` fails specifically inside its internal `ensureVersionTable()` (after already doing `instance.Conn(ctx)`), the golang-migrate pgx/v5 driver itself leaks that `*sql.Conn` — verified by reading `database/pgx/v5/pgx.go`'s `WithInstance`. Unreachable from our code: we never get a handle to that connection on this path, so there is nothing `NewMigrator` could release. Upstream-owned, out of this diff's scope.
5. `migrate.NewWithInstance` fails (68): returns `nil, err`, `sqlDB.Close()` (70) — Finding 1 above (dead code today, wrong object closed if it ever fires).
6. Success (74): fully-populated `&Migrator{m, src, sqlDB}, nil`. No partial-construction state is ever handed to a caller — all-or-nothing.

Conclusion: nothing leaks on paths reachable against the pinned dependency versions.

**Item 3 — source-only Migrator: sound design, not a footgun.** All three fields are unexported, so `&Migrator{src: ...}` is only constructible from inside package `db` — external callers (including `cmd/collab-migrate`) can only get one via the exhaustive `NewMigrator`. The pattern is confined to the package's own white-box tests (idiomatic Go, no "friend class" ceremony needed) and is load-bearing: it lets `TestVersions_CountsTheSetNotTheArithmetic`, `TestVersions_EmptySet`, and `TestHead_MatchesMigrationSet` exercise real production logic with an in-memory `fstest.MapFS` instead of a live Postgres. The `Close()` nil-guard exists specifically to make that safe and is exercised by name. **A small constructor would be BALLAST**: it would wrap one already-idiomatic struct literal, used only by same-package tests, enforcing no invariant (a nil `src` fails loudly and immediately inside the misusing test — the correct failure mode for test code), for a handful of call sites with nothing shared to centralize. Leave it as a struct literal.

**Item 4 — SOLID split: clean, and verified backward-compatible.** `cmd/relay/main.go:63` still calls `dbpkg.RunUp(cfg.DatabaseURL, migrations.FS)` unchanged (that file isn't in this diff), and it still runs before `dbpkg.NewPool(...)` at line 66 — exactly matching `RunUp`'s doc comment. The extraction centralized connection-setup into `NewMigrator` without duplicating it and without touching `RunUp`'s signature. `main()`/`run()` split is also clean: `main()` owns flag parsing/`os.Exit`/stderr — the untestable edges — while `run(args, url, confirm, dir)` is pure args-in/error-out. The one real gap is `printStatus` (Finding 3/MED).

**Item 5 — CLI structure: nothing non-idiomatic.** Hand-rolled flag+switch is proportionate; `flag.Usage` correctly wired to stderr; exit codes follow convention (2 = usage/no-args, 1 = any `run()` failure); stdout carries operator data, stderr carries errors and the dirty-schema WARNING specifically (so piping stdout doesn't swallow it) — deliberate, sound. `force`'s `strconv.Atoi` (allows negatives) vs. `up`/`down`/`goto`'s positive/unsigned parsing is **not** an inconsistency: golang-migrate's own `Force(version int)` treats `-1` as a valid sentinel ("no version" — verified: `if version < -1 { return ErrInvalidVersion }`, migrate.go:365 of the pinned dependency), so `force` is correctly the one verb accepting negatives.

**Item 7 — Makefile/CLI two-layer gate: coherent, not accidental duplication.** The same diff's `server/README.md` says so explicitly ("Destructive verbs are gated twice — `CONFIRM=yes` at the Makefile and `-yes` at the CLI"). Hand-traced the interesting edge: `make db-goto V=0` passes **no** `-yes` (the `db-goto` target only checks `-n "$(V)"`), so it relies entirely on the CLI's own `if v == 0 && !confirm` check (main.go:146-148) to refuse the down-all-equivalent case. Correct layering: the Makefile adds friction under a **different** keyword only for targets that are *unconditionally* destructive (`db-down-all`, `db-force`, `db-reset`) and pre-supplies `-yes` once its own gate passes; for `db-goto`, where destructiveness depends on the argument, the Makefile correctly has no opinion and defers to the CLI's value-dependent check. No bypass: a stray `CONFIRM=yes` on `db-goto` is simply ignored by that recipe.

Duplication otherwise: the `err != nil && !errors.Is(err, migrate.ErrNoChange)` wrap-and-swallow idiom repeats 3× verbatim (`Up:99`, `Down:109`, `Goto:152`; `Goto`'s `v==0` branch correctly delegates to `Down()` rather than re-duplicating). Legitimate extraction candidate, but optional — each occurrence is 2 lines and self-contained, matching the file's stated architecture ("every verb is a thin, named entry point... the mechanism is shared, the policy is not"). Status printing has zero duplication — all six commands funnel through one `printStatus`.

## Checked clean (verified, not just read)

- `go build ./...` clean. `go vet ./...` clean. `gofmt -l` on all 4 reviewed files: no output.
- `go test -v ./internal/db/...`: all 9 non-DB tests **PASS**; all 6 real-Postgres integration tests **SKIP** cleanly with the documented un-skip instructions (not a silent pass).
- `errors.As(err, &short)` on `migrate.ErrShortLimit` (migrate.go:138) is correct and necessary: `ErrShortLimit` is a value-receiver struct carrying a variable `Short` field, constructed fresh per call site and returned unwrapped — `errors.Is` against any fixed value could never match every instance. Traced both producers (`readUp`, `readDown` in the pinned dependency) and confirmed both fire only after `count > 0`, exactly matching the doc comment's claim. No other arm in the diff uses `errors.Is` on a struct/parameterized error; the dependency's other struct error, `ErrDirty`, is correctly left unswallowed.
- `ErrNoVersion = migrate.ErrNilVersion` re-export is trivially correct (same value); exercised across 5 distinct integration-test sites.
- `Close()`'s "sql.DB.Close is safe to call twice" verified against Go 1.26 stdlib (`sql.go:926`, explicit idempotency guard). Its "pgx/v5 driver's Close closes the *sql.DB it wraps" verified against `database/pgx/v5/pgx.go`'s `Postgres.Close()`.
- `sql.Open("pgx", url)` driver name verified against `jackc/pgx/v5/stdlib`'s actual `sql.Register("pgx", ...)`.
- `NewMigrator` correctly uses **no** `defer` for its own resources (ownership transfers to the returned struct on success) — a constructor that deferred cleanup of what it hands off would close the resource the moment it returns. `RunUp`/`run()` correctly `defer mg.Close()` only after confirming `NewMigrator` succeeded.
- Test helpers correctly use `t.Cleanup` (not `defer`) for resources whose lifetime is the whole test (`openMigrator`, `newTestDB`) — and `newTestDB` correctly opens a **fresh** admin connection inside its `t.Cleanup` callback rather than reusing the already-`defer`-closed one from earlier in the function (cleanup callbacks run after the test function's own defers have already fired).
- `os.Stat` + `errors.Is(err, os.ErrNotExist)` in `create()` (main.go:269-273) is the correct modern idiom.
- `RunUp`'s doc-comment claim about `cmd/relay`'s call order verified directly against `cmd/relay/main.go:63,66`.
- No naked returns in either file. `Version()`'s named returns are self-documentation only, never a bare `return`.
- Comment hygiene: every added comment in both files is ARMOR by the CLAUDE.md test — design rationale, safety/ownership argument, or a technical claim I independently verified against dependency/stdlib source or a named test (the `Steps()` comment's citation of golang-migrate's exact internal branch, `PendingAfter`'s "version numbers are not step counts" tied 1:1 to `TestVersions_CountsTheSetNotTheArithmetic`). None restate the line below. Not recommending any trim.
- The `switch { case err == nil, errors.Is(...), errors.As(...), errors.Is(...): }` in `Steps()` is valid, idiomatic Go and compiles.
- The integration suite exercises a real Postgres round trip (tables asserted by name after Up/Down), not output-vs-output self-comparison.

Files referenced: `/private/tmp/claude-501/-Users-kevinduffey-projects-studio/0fe929e6-fb12-463c-b8d9-2d22454974c9/scratchpad/gate-fdc4a7a/server/internal/db/migrate.go`, `.../server/cmd/collab-migrate/main.go`, `.../server/internal/db/migrate_test.go`, `.../server/internal/db/migrate_integration_test.go`, `.../server/Makefile`, `.../server/README.md`.
